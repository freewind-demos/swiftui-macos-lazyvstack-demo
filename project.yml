name: SwiftUILazyVStackDemo
options:
  bundleIdPrefix: com.freewind
  deploymentTarget:
    macOS: "14.0"
settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
targets:
  SwiftUILazyVStackDemo:
    type: application
    platform: macOS
    productName: SwiftUILazyVStackDemo
    sources:
      - Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.freewind.SwiftUILazyVStackDemo
        INFOPLIST_FILE: Sources/Info.plist
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGN_STYLE: Manual
        DEVELOPMENT_TEAM: ""
        ENABLE_HARDENED_RUNTIME: NO
schemes:
  SwiftUILazyVStackDemo:
    build:
      targets:
        SwiftUILazyVStackDemo: all
    run:
      config: Debug
